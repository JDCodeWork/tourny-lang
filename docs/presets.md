# Presets (post‑MVP)

Los presets no son necesarios para el MVP. Están planificados y descritos en `docs/timeline.md`.

Idea:

- `CARGAR "@presets/nombre"` aplica una configuración predefinida.
- Los presets se pueden definir como archivos **YAML**.
